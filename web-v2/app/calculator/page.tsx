import { AuditedCalculator } from '@/components/calculator/audited-calculator';
export const metadata = {title:'Phase calculator',description:'Historical exposure frequency in consecutive coordinate-specific crop intervals.'};
export default function CalculatorPage(){return <section className="relative min-h-[100dvh] bg-bg-deep pt-14"><div className="relative mx-auto max-w-[1400px] px-6 pb-24 pt-16"><header className="max-w-[880px]"><p className="kicker">Phase calculator</p><h1 className="mt-3 font-display text-4xl text-ink">Climate exposures through the crop cycle.</h1><p className="mt-5 text-sm leading-relaxed text-ink-dim">Select an exact 0.5 degree cell, crop and season. Each consecutive calendar interval uses the same estimated dates as the risk calendar. Evaluate the applicable literature-linked triggers separately, including multiple triggers within one combined phase interval.</p><p className="mt-3 text-xs text-ink-dim">Daily and hourly ERA5-Land queries run through Google Earth Engine. Unavailable rules remain visible with their scientific and data limitations. Calendar years refer to planting years.</p></header><AuditedCalculator/></div></section>}
