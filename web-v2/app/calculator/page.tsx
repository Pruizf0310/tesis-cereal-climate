import { RegionalCalculator as AuditedCalculator } from '@/components/calculator/regional-calculator';
export const metadata = {title:'Phase calculator',description:'Historical exposure frequency in consecutive coordinate-specific crop intervals.'};
export default function CalculatorPage(){return <section className="relative min-h-[100dvh] bg-bg-deep pt-14"><div className="relative mx-auto max-w-[1400px] px-6 pb-24 pt-16"><header className="max-w-[880px]"><p className="kicker">Phase calculator</p><h1 className="mt-3 font-display text-4xl text-ink">Climate exposures through the crop cycle.</h1><p className="mt-5 text-sm leading-relaxed text-ink-dim">Choose a crop, latitude band and longitude zone. Screen crop-inventory locations for the selected threat, then calculate its historical frequency within one of six consecutive estimated phases. Duration, threshold and the exact variable remain visible.</p><p className="mt-3 text-xs text-ink-dim">Daily and hourly ERA5-Land queries run through Google Earth Engine. Threats requiring canopy, hydrological or experimental-control measurements accept the specified external CSV. Download series, exposure intervals and provenance for subsequent analysis. Calendar years refer to planting years.</p></header><AuditedCalculator/></div></section>}
